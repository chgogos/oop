#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>

// HEAPSORT

int main()
{
    std::ostream_iterator<int> output(std::cout, " ");

    // make_heap

    // sort_heap

    // push_heap

    // pop_heap

    // is_heap

    // is_heap_until

}
